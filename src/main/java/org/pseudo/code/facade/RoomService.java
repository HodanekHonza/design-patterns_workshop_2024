package org.pseudo.code.facade;

import java.util.UUID;

public class RoomService {
   // check if a room is occupied
    public boolean isRoomOccupied(UUID roomUUID) {
        // This is just a placeholder for actual logic to check room occupancy
        // In a real implementation, this would query the database or room management system
        // For simplicity, let's assume the room is occupied
        return true;
    }
}
